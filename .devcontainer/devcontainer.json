 { "name": "InternTrack API", 
   "image": "mcr.microsoft.com/devcontainers/java:17-bullseye",
    "postCreateCommand": "sudo rm -f /etc/apt/sources.list.d/yarn.list && sudo apt-get update && sudo apt-get install -y postgresql && sudo su - -c \"service postgresql start && su - postgres -c 'psql -c \\\"ALTER USER postgres PASSWORD '\\''postgres'\\'';\\\"' && su - postgres -c 'createdb interntrack'\" && chmod +x mvnw",
  "postStartCommand": "sudo su - -c \"service postgresql start\"",
   "forwardPorts": [8080, 5432] 
}
